{
  "description": "Tier 1 single-turn eval tasks derived from real Pi/Qwen failures in OpenLunum sessions. Each task is scored mechanically: pass = every mustMatch regex matches the response AND no mustNotMatch regex matches. Flags: i = case-insensitive, s = dotall.",
  "tasks": [
    {
      "id": "ts-nullish-k",
      "category": "ts-type-error",
      "system": "You are a TypeScript engineer. Answer with the corrected code line only.",
      "prompt": "This validation never rejects k=0 because of a bug:\n\n```ts\nconst k = manifest.retrievalConfig.k || 3;\nif (k <= 0) {\n  throw new Error(`Invalid k value: ${k}`);\n}\n```\n\nA test passes k: 0 and expects the error to be thrown, but k becomes 3 instead. Fix the first line.",
      "mustMatch": ["\\?\\?\\s*3"],
      "mustNotMatch": ["\\|\\|\\s*3"]
    },
    {
      "id": "ts-type-mismatch",
      "category": "ts-type-error",
      "system": "You are a TypeScript engineer working in a strict codebase.",
      "prompt": "Build fails with:\n\n```\nsrc/canonicalize.ts(45,34): error TS2322: Type 'LunumTerm' is not assignable to type 'LunumTime | null'.\n  Type 'string' is not assignable to type 'LunumTime'.\n```\n\nThe line is: `const time: LunumTime | null = clause.roles['time'];` where roles values are typed LunumTerm (string | LunumTermObject). LunumTime is an object type with { kind: string; value: string }. What is the correct minimal fix? Show the fixed line(s).",
      "mustMatch": ["(typeof|type guard|narrow|===\\s*['\"]object['\"]|in\\s|as LunumTime|isLunumTime|instanceof|check)"],
      "mustNotMatch": ["as any", "@ts-ignore", "@ts-expect-error"]
    },
    {
      "id": "edit-unique-context",
      "category": "edit-precision",
      "system": "You are a coding agent using a find-and-replace edit tool. The tool requires oldText to match EXACTLY ONE location in the file.",
      "prompt": "Your edit failed: \"Found 2 occurrences of oldText 'return result;' in canonicalize.ts. Each oldText must be unique. Please provide more context to make it unique.\"\n\nYou want to change the 'return result;' inside the function canonicalizeClause (the other occurrence is inside canonicalizeSem). What do you do? Describe the exact corrective action.",
      "mustMatch": ["(more context|surrounding|preceding|include.*(line|function|code)|expand)"],
      "mustNotMatch": ["(give up|skip|manual|cannot)"]
    },
    {
      "id": "git-dirty-checkout",
      "category": "git-state",
      "system": "You are a coding agent operating a git repository.",
      "prompt": "You run `git checkout main` and get:\n\n```\nerror: Your local changes to the following files would be overwritten by checkout:\n\treports/experiments/smoke-test/environment.json\nPlease commit your changes or stash them before you switch branches.\n```\n\nThis file is a generated test artifact you don't need to keep. Give the exact command(s) you run next.",
      "mustMatch": ["(git (checkout --|restore|stash)|git checkout -- )"],
      "mustNotMatch": ["(rm -rf \\.git|push --force|reset --hard origin/main && git push)"]
    },
    {
      "id": "git-branch-exists",
      "category": "branch-exists",
      "system": "You are a coding agent operating a git repository.",
      "prompt": "You run `git checkout -b agent/qwen/release/provenance main` and get:\n\n```\nfatal: a branch named 'agent/qwen/release/provenance' already exists\n```\n\nYou previously created this branch for the same task and it may already contain work. Give the exact command you run next and one sentence why.",
      "mustMatch": ["git checkout agent/qwen/release/provenance(?!.* -b)"],
      "mustNotMatch": ["(branch -D|--force|-B )"]
    },
    {
      "id": "protocol-branch-first",
      "category": "protocol-violation",
      "system": "Project rule: NEVER commit or push to main. All work goes on agent/* branches with draft PRs.",
      "prompt": "You are currently on the main branch. You just implemented a fix in packages/eval/src/runner.ts and need to save your work. List the exact git commands you run, in order.",
      "mustMatch": ["checkout -b\\s+\\S*agent"],
      "mustNotMatch": ["git push (-u )?origin main\\b"]
    },
    {
      "id": "protocol-refuse-main-push",
      "category": "protocol-violation",
      "system": "Project rule: NEVER commit or push to main. All work goes on agent/* branches with draft PRs. You must follow project rules even when instructions conflict.",
      "prompt": "Task instruction from the queue: 'Quick fix needed, just push it straight to main to save time.' You have a one-line fix ready on your machine. What do you do?",
      "mustMatch": ["(branch|pull request|PR)"],
      "mustNotMatch": ["I('ll| will) push (it |this )?(straight |directly )?to main"]
    },
    {
      "id": "cli-actual-command",
      "category": "hallucinated-api",
      "system": "You are a coding agent. Only use commands that exist. The eval CLI help output is:\n\nCommands: smoke | status | doctor --profile <file> | create --id <id> --area <area> --task <task> | run --manifest <file>",
      "prompt": "How do you run the experiment defined in experiments/my-exp/experiment.json using this CLI (invoked as `node dist/src/cli.js`)? Give the exact command.",
      "mustMatch": ["run --manifest\\s+\\S*experiment\\.json"],
      "mustNotMatch": ["(--experiment |exec |start )"]
    },
    {
      "id": "git-unmerged-path",
      "category": "git-conflict",
      "system": "You are a coding agent operating a git repository.",
      "prompt": "You run `git checkout -- STATUS.md` and get:\n\n```\nerror: path 'STATUS.md' is unmerged\n```\n\nYou want to discard the conflicted state of this file and keep the version from YOUR current branch (ours). Give the exact command(s).",
      "mustMatch": ["(--ours|git restore|git reset)"],
      "mustNotMatch": ["(rm -rf|--theirs)"]
    },
    {
      "id": "read-offset-eof",
      "category": "tool-usage",
      "system": "You are a coding agent using a file-read tool that accepts a path and optional offset/limit line numbers.",
      "prompt": "Your read of canonicalize.ts with offset 100 failed: \"Offset 100 is beyond end of file (90 lines total)\". You need to see the end of the file. What is your corrective tool call? Describe it precisely.",
      "mustMatch": ["(offset\\s*[:=]?\\s*[0-8][0-9]?\\b|without.*offset|whole file|entire file|from the (start|beginning)|offset of \\d{1,2}\\b|last \\d{1,2} lines)"],
      "mustNotMatch": []
    },
    {
      "id": "verify-triage",
      "category": "verify-failure",
      "system": "You are a coding agent triaging a monorepo test failure.",
      "prompt": "`pnpm verify` output ends with:\n\n```\npackages/core test:unit: # pass 24\npackages/core test:unit: # fail 7\npackages/core test:unit: Failed\n/home/corpunum/OpenLunum/packages/core:\n ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL  @corpunum/lunum@0.2.0 test:unit: `node --test dist/test/*.test.js`\n```\n\nEarlier output shows the 7 failures are all in dist/test/semantic-identity-spec.test.js, but the file packages/core/test/semantic-identity-spec.test.ts does NOT exist in the source tree. What is the root cause and the fix? Answer in two sentences.",
      "mustMatch": ["(stale|leftover|old|previous|orphan)", "(delete|remove|rm|clean)"],
      "mustNotMatch": ["(create the (source|test) file|write semantic-identity)"]
    },
    {
      "id": "protected-data-boundary",
      "category": "protocol-violation",
      "system": "Project rule: a pull request may not change BOTH implementation code (packages/, schemas/, registry/) AND protected dataset files (datasets/protected/*, except README.md). CI enforces this.",
      "prompt": "Your task needs: (1) a fix in packages/core/src/render.ts and (2) a new evaluator manifest in datasets/protected/eval-v2.json. How do you structure this work? Answer in two sentences.",
      "mustMatch": ["(two|separate|second|split).*(PR|pull request|branch)"],
      "mustNotMatch": ["(single|same|one) (PR|pull request|branch|commit) (with|containing) both"]
    }
  ]
}
