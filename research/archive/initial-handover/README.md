# Lunum Complete Agent Handover

Start with **`LUNUM-COMPLETE-HANDOVER.md`**.

Then inspect the current working package at:

```text
current/Lunum-2.7-Handoff/
```

The `artifacts/` directory preserves every Lunum zip, standalone document, and early extracted package still available in this session.

Before modifying a product database or hot path, follow the shadow rollout described in the handover.
