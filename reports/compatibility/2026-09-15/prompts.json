{
  "schema": "openlunum-client-compatibility-prompts/0.1",
  "codex": "Use only the configured OpenLunum MCP server. Do not use shell, filesystem, network, or any other tool. Perform these exact conformance steps in order: (1) call lunum_get_extraction_contract; (2) call lunum_build_candidate with world real, kind preference, predicate prefer, and roles experiencer={type:actor,id:maria}, theme={type:concept,id:quiet_mode}; (3) take the returned candidate exactly and call lunum_submit_candidate with sourceText \"Maria prefers quiet mode.\", sourceLanguage \"en\", that candidateSem, and provenance {extractorType:\"codex_agent\"}; (4) call lunum_submit_candidate with candidateSem null, sourceText \"Unsupported.\", sourceLanguage \"en\", provenance {extractorType:\"codex_agent\"}; (5) call lunum_compare with expected equal to the built candidate and actual equal to the same candidate except its clause negated=true, and report the critical mismatch; (6) call lunum_submit_candidate with sourceText \"Maria prefers quiet mode.\", sourceLanguage \"en\", candidateSem \"malformed\", provenance {extractorType:\"codex_agent\"}. Report only observed tool-call outcomes. Do not claim a tool call unless its MCP event occurred.",
  "claude": "Use only the configured OpenLunum MCP server. Do not use shell, filesystem, network, or any other tool. Perform these exact conformance steps in order: call lunum_get_extraction_contract; call lunum_build_candidate with world real, kind preference, predicate prefer, and roles experiencer={type:actor,id:maria}, theme={type:concept,id:quiet_mode}; submit the returned candidate exactly with sourceText \"Maria prefers quiet mode.\", sourceLanguage \"en\", and provenance {extractorType:\"claude_agent\"}; submit one explicit abstention with candidateSem null, sourceText \"Unsupported.\", sourceLanguage \"en\", and the same provenance; call lunum_compare with the built candidate as expected and a copy with clause negated=true as actual; do not call any other tool. Report only observed MCP tool results and do not claim calls without MCP events.",
  "agy": "Use only the configured OpenLunum MCP server. Do not use shell, filesystem, network, or any other tool. Perform these exact conformance steps in order: call lunum_get_extraction_contract; call lunum_build_candidate with world real, kind preference, predicate prefer, and roles experiencer={type:actor,id:maria}, theme={type:concept,id:quiet_mode}; submit the returned candidate exactly with sourceText \"Maria prefers quiet mode.\", sourceLanguage \"en\", and provenance {extractorType:\"agy_agent\"}; submit one explicit abstention with candidateSem null, sourceText \"Unsupported.\", sourceLanguage \"en\", and the same provenance; call lunum_compare with the built candidate as expected and a copy with clause negated=true as actual; do not call any other tool. Report only observed MCP tool results and do not claim calls without MCP events."
}
