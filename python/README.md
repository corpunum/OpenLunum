# Python research workspace

Python is optional and reserved for model, tokenizer, corpus, statistics, and future fine-tuning work. Protocol behavior must be captured in shared JSON Schema and conformance vectors so the TypeScript reference implementation and other languages can reproduce it.
